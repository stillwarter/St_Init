# Vite 开发服务器端口（与 wait-electron、主进程加载地址需一致）
VITE_DEV_SERVER_PORT=5173

# 主进程开发模式下打开的页面（修改端口时请同步改上一行）
DEV_SERVER_URL=http://localhost:5173

# 是否自动打开 DevTools（1=打开，0=关闭）
OPEN_DEVTOOLS=1

# 是否过滤 DevTools Autofill 协议噪声（1=过滤，0=不过滤）
FILTER_DEVTOOLS_NOISE=1
